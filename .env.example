# Neon Postgres / hosted Postgres connection string
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require

# Optional: set to false only for a local Postgres instance that does not use SSL.
# DATABASE_SSL=false
